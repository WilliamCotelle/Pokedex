<%- include('partials/head') %>
<%- include('partials/header') %>

<div class="content-container">
  <h1 class="content-title">Types de Pokémon</h1>

  <div class="types-container">
    <% types.forEach(t => { %>
      <a href="/type/<%= encodeURIComponent(t) %>" class="type-card <%= 'type-' + t.toLowerCase() %>">
        <div><%= t %></div>
      </a>
    <% }); %>
  </div>
</div>

<%- include('partials/footer') %>
